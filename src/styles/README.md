# StudioLedger design versions

- **version 00** — Archived in `archive/version-00/`. Visual research phase; dashboard dark mode was the reference. **Preview:** [http://localhost:3000/version-00](http://localhost:3000/version-00). Restore by copying `version-00-globals.css` to `src/app/globals.css` and (if needed) applying `tailwind-extension-snapshot.ts` into `tailwind.config.ts`.
- **Version 1.0** — Current. Single token set for dashboard + landing, Outfit for headings, theme-aware sidebar, primary action strip, escrow prominence, empty-state copy, staggered load.
